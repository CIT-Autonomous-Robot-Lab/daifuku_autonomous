# プロジェクト名は固定する。名前付きボリュームがこれを接頭辞に持つので、
# ディレクトリを動かすと colcon のキャッシュが別ボリュームに化ける。
name: daifuku-raspicat-dev

services:
  raspicat-dev:
    build:
      context: ../..
      dockerfile: docker/dev/Dockerfile
    image: daifuku-raspicat-dev:humble
    container_name: daifuku-raspicat-dev
    network_mode: host
    stdin_open: true
    tty: true
    command: ["sleep", "infinity"]
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined
    environment:
      DISPLAY: ${DISPLAY:-host.docker.internal:400.0}
      WAYLAND_DISPLAY: ${WAYLAND_DISPLAY:-}
      XDG_RUNTIME_DIR: ${XDG_RUNTIME_DIR:-}
      LIBGL_ALWAYS_SOFTWARE: ${LIBGL_ALWAYS_SOFTWARE:-1}
      QT_X11_NO_MITSHM: "1"
      ROS_DOMAIN_ID: ${ROS_DOMAIN_ID:-90}
      ROS_LOCALHOST_ONLY: ${ROS_LOCALHOST_ONLY:-0}
      RMW_IMPLEMENTATION: ${RMW_IMPLEMENTATION:-rmw_fastrtps_cpp}
      # Pi 上のネイティブな ROS 2 ノードを、Fast DDS の共有メモリで同一ホストの
      # 相手と取り違えさせない。
      FASTDDS_BUILTIN_TRANSPORTS: ${FASTDDS_BUILTIN_TRANSPORTS:-UDPv4}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ../..:/workspaces/daifuku_autonomous
      - autonomous-build:/workspaces/daifuku_autonomous/build
      - autonomous-install:/workspaces/daifuku_autonomous/install
      - autonomous-log:/workspaces/daifuku_autonomous/log

volumes:
  autonomous-build:
  autonomous-install:
  autonomous-log:
