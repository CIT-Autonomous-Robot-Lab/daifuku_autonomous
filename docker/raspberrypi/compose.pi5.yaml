# Raspberry Pi 5 で走らせるときの上書き。compose.yaml に重ねて使う。
#
#   docker compose -f docker/raspberrypi/compose.yaml \
#                  -f docker/raspberrypi/compose.pi5.yaml up -d
#
# 変えるのは raspicat サービスだけ。ナビゲーション側 (ros2 サービス) は
# Pi 4 と同じで、機種に依存しない。
#
# Pi 5 では rtmouse カーネルモジュールが動かない (BCM2711 のレジスタを ioremap
# するが、GPIO/PWM は RP1 側にある)。そのため /dev/rt* は存在せず、代わりに
# autonomous_nav の raspicat_pi5_driver.py が RP1 のハードウェア PWM・gpiochip・
# I2C をユーザ空間から直接叩く。経緯と検証項目は docs/setup/raspberry-pi-5.md。
#
# ステップクロックは /sys/class/pwm から出す。カーネルの PWM サブシステムには
# キャラクタデバイスの API が無いので、sysfs を通すしかない。Docker は既定で
# /sys を read-only で見せるため、必要な 2 つを rw で重ねる。
#
#   * /sys/class/pwm    … pwmchip* と export/unexport
#   * /sys/devices      … /sys/class/pwm/pwmchipN は実体へのシンボリックリンクで、
#                         こちらが無いとリンクが宙に浮く
#
# /sys/devices を丸ごと rw で渡すのは広い。ただし compose.yaml は既にモータ制御の
# ために /dev を丸ごと渡し `device_cgroup_rules: c *:* rwm` を付けており、キャラクタ
# デバイスについてはほぼ privileged 相当になっている (compose.yaml のコメント)。
# ここもその延長で、狭めるなら pwmchip の実体パスだけを名指しでマウントする
# (PCIe のアドレスがパスに入るので機種ごとに書き換えが要る)。
#
# コンテナは user: "1000:1000" で動くので、ホスト側で sysfs の所有権を渡しておく
# 必要がある。tools/image/udev/99-daifuku-pwm.rules を参照。

services:
  raspicat:
    command:
      - /ros_entrypoint.sh
      - ros2
      - launch
      - autonomous_nav
      - robot_bringup.launch.py
      - driver:=pi5
    # Compose はマウント先をキーにして重ねるので、ここは追加分だけでよい
    # (compose.yaml 側の /dev などはそのまま残る)。
    volumes:
      - /sys/class/pwm:/sys/class/pwm
      - /sys/devices:/sys/devices
