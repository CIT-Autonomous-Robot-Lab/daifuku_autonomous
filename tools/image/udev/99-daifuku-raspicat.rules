# 自前の本体ドライバ (raspicat_driver) をコンテナから動かすための権限。
# Pi 4 でも Pi 5 でも同じで、必要な資源も同じ (PWM / gpiochip / i2c-dev)。
#
# docker/raspberrypi/compose.yaml の raspicat サービスは user: "1000:1000" で走る。
# コンテナには補助グループが引き継がれないので、ホストで gpio や i2c グループへ
# chgrp しても届かない。所有者そのものを 1000:1000 にするのが確実。
#
# 入るのは tools/image/provision.sh 経由 (機種によらず入る。公式実装 +
# rtmouse で動かす場合は使われないだけで、害は無い)。反映は
#   sudo udevadm control --reload && sudo udevadm trigger
# または再起動。

# ステップクロック。/sys/class/pwm/pwmchipN/export を書くと pwmN/ が生えるが、
# これは子デバイスの add では飛ばず、pwmchipN 自身への ACTION=="change" として
# 飛ぶ (Pi 5 実機 2026-08-04、udevadm monitor で実測)。add だけを見ていると
# pwmN/ は root のまま残り、ドライバが period / duty_cycle を書けない。
# %p は devpath で、/sys%p が実体のディレクトリ (/sys/class/pwm/ の下は
# シンボリックリンクなので chown -R では辿らない)。
SUBSYSTEM=="pwm", ACTION=="add|change", RUN+="/bin/sh -c 'chown -R 1000:1000 /sys%p'"

# モータの方向線と電源線。Pi 4 の pinctrl-bcm2835 と Pi 5 の pinctrl-rp1 の
# どちらも拾えるよう全 gpiochip を対象にする (番号はカーネル版で動くうえ、
# ドライバはラベルで選ぶため)。
SUBSYSTEM=="gpio", KERNEL=="gpiochip[0-9]*", OWNER="1000", GROUP="1000", MODE="0660"

# パルスカウンタ (0x10 / 0x11)。
SUBSYSTEM=="i2c-dev", OWNER="1000", GROUP="1000", MODE="0660"
