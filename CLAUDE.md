# CLAUDE.md

指針は [`AGENTS.md`](AGENTS.md) にあります。このファイルは取り込むだけで、中身は
持ちません（**二重に持つと片方が古くなる**）。symlink にしていないのは、開発ホストの
Windows が `core.symlinks=false` で、そちらのチェックアウトでは中身が `AGENTS.md` の
一行テキストになり**指針が丸ごと消えるため**。

@AGENTS.md
